// BACKUP of original homepage prior to 2026-04-22 stage-based redesign.
// Restore by renaming this file back to page.tsx.

import Link from "next/link"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@/components/ui/card"
import { CheckCircle } from "lucide-react"
import NewsletterSignup from "@/components/newsletter-signup"
import { EmailCapture } from "@/components/email-capture"
import FeaturedBlogs from "@/components/featured-blogs"
import HeroSection from "@/components/hero-section"

export default function Home() {
  return (
    <div className="flex min-h-screen flex-col">
      <main className="flex-1">
        <HeroSection />
        <section className="bg-gray-50 py-12">
          <div className="container px-4 md:px-6">
            <div className="mb-10 text-center">
              <h2 className="text-3xl font-bold tracking-tight">Everything You Need Before the Demo Starts Talking Back</h2>
              <p className="mx-auto mt-4 max-w-3xl text-muted-foreground">
                Guides, planning tools, and research support for every phase of a renovation.
              </p>
            </div>
          </div>
        </section>
      </main>
    </div>
  )
}
